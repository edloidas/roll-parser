{
  "$schema": "https://json.schemastore.org/tsconfig",
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "lib": ["ES2022", "DOM", "DOM.Iterable"]
  },
  "include": ["src/**/*.ts", "../scripts/build-site.ts", "../scripts/serve-site.ts"],
  "exclude": ["node_modules", "dist"]
}
